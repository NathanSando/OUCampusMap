PORT=3000
SUPABASE_URL=
# Bypasses Row Level Security. Server-side only — never put this in the mobile app.
SUPABASE_SERVICE_ROLE_KEY=
AI_SERVICE_URL=http://localhost:8000
AI_SERVICE_SHARED_SECRET=
# Comma-separated list of allowed browser origins (optional; native apps don't send Origin).
CORS_ORIGINS=
